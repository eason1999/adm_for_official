let _apiServer_ = '/api';

// 是否请求API服务器：true为请求，false为不请求
let _publish_ = true;

let config = {
  login: '/adm/login',
  index: '/adm/adv/campaign',
  apiServer: _apiServer_,
  publish: _publish_
};

export default config;
